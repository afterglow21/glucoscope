{
  "name": "glucoscope-usage-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/usage-core.js && node --check src/d1-store.js && node --check src/credentials.js && node --check src/turnstile.js && node --check test/usage-core.test.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test",
    "types": "wrangler types",
    "types:check": "wrangler types --check",
    "dev": "wrangler dev --local --test-scheduled",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
