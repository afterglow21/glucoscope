{
  "name": "gluco-letter-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/letter-quality.js",
    "deploy:dry": "wrangler deploy --dry-run",
    "test:quality": "node --test test/letter-quality.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
