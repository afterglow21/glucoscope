{
  "name": "glucoscope-native",
  "version": "0.1.0",
  "private": true,
  "description": "Unsigned iOS feasibility build for GlucoScope",
  "scripts": {
    "build:web": "node scripts/prepare-web.mjs",
    "cap:add:ios": "npm run build:web && npx cap add ios",
    "cap:sync:ios": "npm run build:web && npx cap sync ios",
    "test": "node --test ../test/native-app-spike.test.mjs"
  },
  "dependencies": {
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@capacitor/ios": "8.5.0"
  }
}
